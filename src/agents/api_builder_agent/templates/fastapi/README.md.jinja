# {{ project_name }}

Generated by **AI API Builder**.

## Stack

- FastAPI + SQLAlchemy 2.0
- Database: {{ database }}
{% if use_jwt %}
- Auth: JWT (OAuth2 password flow)
{% endif %}
- Docker + docker-compose
- pytest

## Entities

{% for e in entities %}
- `{{ e.name }}` — CRUD at `/{{ e.plural }}`
{% endfor %}

## Run locally

```bash
python -m venv .venv
. .venv/bin/activate      # Windows: .venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Open http://localhost:8000/docs for Swagger UI.

## Run with Docker

```bash
docker compose up --build
```

## Tests

```bash
pytest
```
{% if use_jwt %}

## Authentication

1. `POST /auth/register` with `{"username": "...", "password": "..."}`
2. `POST /auth/token` (form-encoded) to obtain a bearer token
3. Send `Authorization: Bearer <token>` on entity routes
{% endif %}
